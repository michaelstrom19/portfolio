the three defines at the top turn on and off the shaders, when setting comic shading to 1, also set toon sahding to 1 for better effect.

if everything is at 0 that it pretty much the default that came with the scene. I creaded none of the scenes, just the shader applications.
